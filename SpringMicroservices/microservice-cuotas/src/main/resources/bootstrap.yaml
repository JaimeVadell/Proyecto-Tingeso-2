spring:
  application:
    name: cuotas-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/cuotas
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
