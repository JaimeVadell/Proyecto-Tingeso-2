spring:
  application:
    name: estudiante-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/estudiantes
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

